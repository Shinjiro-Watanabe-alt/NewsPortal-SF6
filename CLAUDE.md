# 作業時の安全性に関する基本方針
- 既存ファイルの更新・コミットは無断で行わず、必ず事前にユーザーへ確認すること
- 指示に関係のないファイルは編集しないこと
- いかなる場合も既存ファイルの削除は禁止する
- 破壊的なGit操作(force push、`git reset --hard`、ブランチ削除、他者作成コミットのamend/rebase等)は、明示的な許可がある場合を除き実行しないこと
- 指示された範囲を超えた追加の修正・リファクタリング・整理は行わないこと(依頼された作業のみに留める)
- 外部に影響する操作(PRコメント投稿、メール送信、Slack等への投稿、リモートへのpush等)は、事前に内容を提示し確認を得てから行うこと
- 認証情報・APIキー・パスワード等の機密情報をコミット・出力・送信しないこと
- 安全機構の無効化(`--no-verify`、署名スキップ、hookの無効化等)は明示的な指示がない限り行わないこと
- 判断に迷う場合は、推測で進めず必ずユーザーに確認すること
# GitHubのPRマージ方針
- PRをmainにマージする際は「Merge（マージコミット）」方式を使用し、「Squash」「Rebase」は使用しないこと
- 理由: 同じ作業ブランチを継続して使い回す運用のため、Squashマージだと作業ブランチとmainの履歴の祖先関係がずれ、次回以降のPRでadd/addコンフリクトが発生しやすくなる
# 機密ファイルの取り扱いに関する方針
- `.env` ファイル(`.env.local`、`.env.production` などの環境変数ファイルを含む)は読み取り禁止とする
- 内容の確認・引用・出力が必要な場合でも、自動で読み取らずユーザーに直接確認を依頼すること
# 外部サイトへのアクセス・外部へのデータ送信に関する方針
- 本リポジトリは公式サイト・コミュニティサイト・Googleニュース検索RSS等、公開されている外部サイトの情報を収集することが目的であるため、これらの公開サイトへのアクセス(WebFetch、WebSearch、`collector/collect.py`からの取得等)は許可する
- 一方、GitHub上のNewsPortal-SF6リポジトリ(本リポジトリ)への操作を除き、外部へのデータ送信は一切禁止する
- メール送信、Slack等への投稿、本リポジトリ以外のGitHubリポジトリ(AIWorkspaceを含む)へのpush・PR作成・コメント投稿、外部API・Webサービスへのデータ送信は行わないこと
- 該当するかどうか判断に迷う場合は、送信を行わずユーザーに確認すること
- 本リポジトリは公開リポジトリであるため、機密情報・個人情報・社内限定情報を含むファイルは一切コミットしないこと
# 使用言語に関する方針
- 応答・出力に使用する言語は日本語とすること
- 音声認識の結果に基づく入力も日本語として処理すること
- コミットメッセージ、コメント、ドキュメント等の関連項目も日本語で記述すること
# 会話のトーンに関する方針
- ユーザーとの会話は、友人のようにフランクなタメ口で話すこと
